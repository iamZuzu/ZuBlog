// Edit these two lines to rename your blog / change the tagline shown in
// the browser tab and page metadata. That's the only place it's defined —
// both the header and the page <title> read from here.
export const SITE_NAME = "My Blog";
export const SITE_DESCRIPTION = "Thoughts, notes, and posts.";

// Set this to your real address once you've deployed (e.g. the Netlify URL
// or your own domain), with no trailing slash. It's used to build the
// sitemap, the canonical link on each page, and the preview image/link
// shown when a post is shared on social media. Nothing breaks if you leave
// the placeholder — sharing previews just won't have a fully correct URL
// until you update it.
export const SITE_URL = "https://example.com";
